/*
    Ejercicio
    El siguiente fragmento de código utiliza un concepto que todavía no vimos 
    en la carrera. Copia este fragmento en un archivo de pruebas 
    y ejecutalo con Node.
*/


const frutas = ['manzana', 'banana', 'pera', 'naranja']; // Se define un array con frutas

for (const fruta of frutas) { // Se inicia un bucle que recorre cada fruta en el array
  console.log(fruta); // Imprime cada fruta en la consola
}

